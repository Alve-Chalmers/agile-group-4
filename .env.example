# Copy to the paths below (never commit real secrets).

# apps/mobile/.env — Expo public vars (also configurable via EAS / `expo start`)
# EXPO_PUBLIC_API_URL=http://localhost:3000

# apps/api/.env — server secrets (see apps/api/.env.example)
